# 技术测试用例：interview-loop 开源隔离实现

> 实现员从**实际实现**推导的技术 / 集成侧测试用例，补充 `business-test-cases.md`（业务侧）。
> 执行由门禁执行器负责（`scripts/check-pii.sh` + clone 走查）；本文件只定义用例（输入 + 预期）。
>
> 注：为避免把真实指纹写进仓库，本文件一律用「真实姓名清单 / 真实公司名 / 真实业务指纹」
> 指代 `scripts/check-pii.sh` 里维护的具体正则模式，不重复罗列字面量。

---

### TC-T01: PII 扫描覆盖工作树 + git 历史
- **类型**: 集成 / 自动化
- **输入**: 仓库工作树与 `git rev-list --all` 全部提交
- **预期**: `scripts/check-pii.sh` 对真实姓名 / 公司 / 业务指纹 / 私有路径扫描，工作树与历史命中数均为 0，exit 0。
- **关联**: 业务 TC-001 / TC-002

### TC-T02: PII 扫描器自排他（不误报自身字面量）
- **类型**: 边界
- **输入**: `scripts/check-pii.sh` 自身源码（含被禁模式的字面量定义）
- **预期**: 工作树扫描通过 `--exclude=check-pii.sh`、历史扫描通过 pathspec `:(exclude)scripts/check-pii.sh` 排除自身；脚本不因匹配自己的模式定义而误报。
- **备注**: 防止「扫描器把自己判成泄漏」的退化。

### TC-T03: PII 扫描器对新增 PII 命中即 exit 1（防呆）
- **类型**: 错误
- **输入**: 在仓库根（非 `data/`、非 `skill/references/`）放一个含真实姓名 / 公司 / 私有路径 / 业务指纹的文件
- **预期**: `scripts/check-pii.sh` 命中并 exit 1（即便 `.gitignore` 未覆盖该位置）。
- **关联**: 业务 TC-011（三重防护中扫描层独立兜底）

### TC-T04: `.gitignore` 白名单保留 `data/` 占位
- **类型**: 边界 / 自动化
- **输入**: `.gitignore` 中的 `/data/*` + `!/data/.gitkeep` + `!/data/README.md`
- **预期**: `git check-ignore data/真实简历.md` 命中（被忽略）；`git check-ignore data/.gitkeep` 与 `data/README.md` 不命中（被白名单保留）；`data/` 目录结构存在于仓库。
- **关联**: 业务 TC-006 / TC-007

### TC-T05: `.gitignore` 不误伤 `examples/` 合法示例
- **类型**: 边界
- **输入**: `examples/resume/林沐-简历-示例.md`（文件名含「简历」）
- **预期**: `git check-ignore` 不命中（被跟踪）。仓库**未使用** `*简历*.md` 这类宽泛通配符。
- **关联**: design.md §2 设计取舍

### TC-T06: 仓库不含产物 / OS 元数据
- **类型**: 业务 / 自动化
- **输入**: 仓库根
- **预期**: 不存在 `interview-loop.zip`、`.DS_Store`、`__MACOSX/`（`ls` + gitignore 双保险）。
- **关联**: 业务 TC-008

### TC-T07: skill 内部零绝对私有路径
- **类型**: 业务 / 可移植
- **输入**: `skill/SKILL.md`、`skill/references/*.md`
- **预期**: 不出现 `/Users/` 开头的绝对路径；reference 引用为相对 `references/`，输出目录为相对 `面试分析/`。
- **关联**: 业务 TC-009

### TC-T08: skill 可整体复制安装
- **类型**: 集成
- **输入**: `skill/` 目录
- **预期**: `skill/SKILL.md` frontmatter 含 `name: interview-loop`；`skill/references/` 含 7 个示例文件；`cp -R skill <skills>/interview-loop`（把 `skill/` 整体复制成 skills 目录下的 `interview-loop/`）或软链后可被 runtime 识别。
- **关联**: 业务 TC-003

### TC-T09: examples 自洽可驱动 skill
- **类型**: 集成
- **输入**: `examples/jd/后端架构AI工程化-示例JD.md` + `examples/resume/林沐-简历-示例.md`
- **预期**: JD 与简历角色 / 公司 / 项目自洽（同属虚构「星澜科技」「林沐」场景），可驱动 skill 完成 Step 2（JD 解析）或 Step 3（候选人分析）。
- **关联**: 业务 TC-004

### TC-T10: examples 显式声明虚构
- **类型**: 业务 / 合规
- **输入**: `examples/README.md`
- **预期**: 文件含「全部为虚构…与任何真实人物 / 公司无关」的中英文声明。
- **关联**: 业务 TC-005

### TC-T11: CI 把 PII 扫描作为硬门禁
- **类型**: 流程
- **输入**: `.github/workflows/pii-scan.yml` + 任意 PR
- **预期**: workflow `fetch-depth: 0`（保留全历史），`bash scripts/check-pii.sh` 非零退出阻断合并。
- **关联**: 业务 TC-010

### TC-T12: 仓库精简（文件数受控）
- **类型**: 边界
- **输入**: 仓库已跟踪文件
- **预期**: 不含构建产物与冗余；根 meta 文件齐全（`README.md` / `LICENSE` / `.gitignore` / `.gitattributes`）。
- **关联**: PRD 成功指标「文件数 ≤ 20」精神（首版）

---

## 覆盖度自检
- 集成 / 可移植：TC-T08 / TC-T09
- 边界（白名单 / 自排他 / 不误伤）：TC-T02 / TC-T04 / TC-T05 / TC-T06 / TC-T12
- 错误 / 防呆：TC-T03
- 流程门禁：TC-T01 / TC-T11
- 合规声明：TC-T10
- 可移植契约：TC-T07

---

## 软性素质对比分析（三段闭环）— 实现侧补充用例

> 以下用例由阶段 3 实现员从**刚写的实现**推导，补充业务侧 `business-test-cases.md`（TC-001~TC-032）。
> 覆盖集成点、文件签名、数据流、错误路径。多数可机械验证（文件存在 / grep / 引用一致）。

### TC-T13: 新增归一化词典文件存在且结构完备
- **类型**: 集成 / 自动化
- **输入**: `skill/references/软性素质词典-参考.md`
- **预期**: 文件存在；含「来源（O*NET Work Styles 公共领域 + WEF 2025 + 国内 HR 术语）」「与现有元能力五项的映射」「~10 个归一化标签（每个含 定义 / 同义词 / O*NET 映射 / 映射元能力 / 考察提示）」；显式声明「不是固定要求清单，只做归一化」与「零心理测评」。
- **关联**: 业务 TC-006 / TC-007 / TC-008

### TC-T14: 新增「要求 × 表现对比」示例文件存在且三态齐全
- **类型**: 集成 / 自动化
- **输入**: `skill/references/软性素质要求×表现对比-示例.md`
- **预期**: 文件存在；含五段（① JD 要求清单 ② 简历证据 ③ 要求×表现对比 ④ 横向对比 ⑤ 证据覆盖校准）；第 ③ 段同时出现 ✅有证据支撑 / ❌缺失 / ❓存疑 三态样例；每条判定引用简历或面试原文。
- **关联**: 业务 TC-014 / TC-015 / TC-030

### TC-T15: SKILL.md 七步集成点描述一致
- **类型**: 集成 / 自动化（grep）
- **输入**: `skill/SKILL.md`
- **预期**: Step 2 含「软性素质要求挖掘（JD 驱动）」子节；Step 3c 标「要求驱动」；Step 4 含「软性素质考察重点」；Step 5 含「软性素质缺口定向探查」；Step 6 含「软性素质要求 × 表现 逐项对比」；Step 7 含「7e. 软性素质证据覆盖校准」；评价哲学含「原则四」。各 AC（1/3/4/5/6/7）在文中可被 grep 到。
- **关联**: 业务 AC-1 / AC-3 / AC-4 / AC-5 / AC-6 / AC-7

### TC-T16: skill 内新增文件数受控（C-8）
- **类型**: 边界 / 自动化
- **输入**: `skill/references/` 目录
- **预期**: 本期 skill 内新增文件 = 2（`软性素质词典-参考.md` + `软性素质要求×表现对比-示例.md`）≤ 3 上限；其余均为现有文件改动，不破坏「精简」约定。
- **关联**: 业务 TC-032 / C-8

### TC-T17: JD 软性素质要求清单六要素签名完备（AC-1）
- **类型**: 集成 / 签名
- **输入**: `软性素质要求×表现对比-示例.md` 第 ① 段清单表头
- **预期**: 每项要求含 名称 / 权重 / 显式 or 隐式 / JD 原文出处 / 定义 / 考察提示 六要素；隐式项注明「推断：…」依据；不出现 JD 未要求的维度。
- **关联**: 业务 TC-001 / TC-002 / TC-003 / M1 / M2

### TC-T18: 三态判定语义正确且存疑中性
- **类型**: 业务 / 关键（数据流）
- **输入**: 第 ③ 段对比表的「综合判定」列
- **预期**: ✅有证据支撑（有原文 + 元能力正向）/ ❌缺失（全资料无证据，真实缺口）/ ❓存疑（单点 / 模糊 / 矛盾）三态与情形一一对应；「存疑」在风险点表与正文均标为「中性，不扣分」。
- **关联**: 业务 TC-015 / TC-016 / C-5

### TC-T19: 证据驱动数据流——无原文不下结论 + ❌缺失 vs ❓未覆盖 切分
- **类型**: 错误 / 防呆（数据流）
- **输入**: 第 ③ 段「学习能力（❓存疑）」「抗压（❓未覆盖）」两行
- **预期**: 学习能力——面试探到迁移思考但未直接探到学习意愿 → ❓存疑（不臆测为「具备」）；抗压——**面试全程未涉及**（压根没探到，非问过还没有）→ ❓未覆盖（中性，反哺 Step 5），**不得标 ❌缺失**；两条均不编造证据，引用原文或显式标「无对应证据」。❌缺失只用于「已探查过仍无证据」。
- **关联**: 业务 TC-016 / C-5 / 现有「无法验证 = 中性」原则

### TC-T20: 横向对比按同一份 JD 清单对齐
- **类型**: 集成
- **输入**: `软性素质要求×表现对比-示例.md` 第 ④ 段 + `候选人横向对比-示例.md` 新增「软性素质横向对比」段
- **预期**: 两处横向表均按同一份 JD 软性素质要求清单逐项对齐（行 = JD 要求，列 = 候选人）；显式声明「不同岗位 / 不同层级不混比」。
- **关联**: 业务 TC-018 / TC-019 / AC-6 / M6

### TC-T21: 零心理测评（校正方向硬边界）
- **类型**: 错误 / 合规（自动化 grep）
- **输入**: `skill/` 全部新增 / 改动内容
- **预期**: 不含任何心理测评问卷题项 / 量表 / 常模 / 百分位；词典只引用 O*NET Work Styles（公共领域分类法）与 WEF 2025（公开报告），二者均非测评工具；无 Likert 题项、无「得分 / 百分位 / 常模」式测量表述。
- **关联**: 业务 TC-025 / NG-1 / M8 / C-1

### TC-T22: 隐私契约不回归（新增 references 零绝对路径）
- **类型**: 业务 / 可移植（回归）
- **输入**: `skill/references/软性素质词典-参考.md`、`软性素质要求×表现对比-示例.md`
- **预期**: 不出现 `/Users/`、`C:\` 绝对路径；内部引用为相对 `references/`；示例数据落 `examples/`（虚构），输出落相对 `面试分析/`。
- **关联**: 业务 TC-026 / TC-T07 / C-6

### TC-T23: 错误路径——JD 模糊不臆造、缺口进风险点
- **类型**: 错误 / 防呆
- **输入**: SKILL.md Step 2「软性素质要求挖掘」+ Step 6「风险点处理」
- **预期**: Step 2 描述含「JD 描述极度模糊 / 缺失时不强行编造，主动向需求方确认或标『JD 未明确，待确认』」；Step 6 缺失（❌）项进风险点并按层级处理（实习生诚信 = 一票否决；外包可管理性 = 强减分；专家 = 提示培养 / 搭配）。
- **关联**: 业务 TC-004 / TC-017 / AC-8

### TC-T24: examples 自洽可驱动软性素质全链路
- **类型**: 集成 / clone-and-run
- **输入**: `examples/jd` + `examples/resume` + `examples/transcript` + `examples/README.md`
- **预期**: JD / 简历 / 面试记录同属虚构「星澜科技 / 林沐」场景，自洽可驱动「JD 要求挖掘 → 简历证据挖掘 → 定向探查 → 要求×表现对比 → 横向对比」全链路；`examples/README.md` 声明该全链路并交叉引用 `软性素质要求×表现对比-示例.md`；无真实数据依赖。
- **关联**: 业务 TC-029 / TC-030 / AC-12 / M10

### TC-T25: references 交叉引用一致
- **类型**: 集成 / 自动化（grep）
- **输入**: `skill/SKILL.md` 参考文件表 + 正文引用
- **预期**: SKILL.md 引用的 `软性素质词典-参考.md`、`软性素质要求×表现对比-示例.md` 文件名与 `skill/references/` 下实际文件完全一致；`面试策略手册.md`、`候选人横向对比-示例.md`、`examples/README.md` 对二者的交叉引用文件名一致。
- **关联**: 业务 TC-014 / TC-029 / TC-T08

---

## 软性素质优化项（第二轮）— 实现侧补充用例

> 以下用例由阶段 3（第二轮）实现员从落地的优化项推导，覆盖隐式高权重自动探查 / AI 润色三联征 / 判定符号统一 / ❌ vs ❓ 切分 / 库外脱敏契约。多数可机械验证（grep）。

### TC-T26: 隐式高权重软性素质要求默认进 Step5 必探查池（AC-4 加强）
- **类型**: 集成 / 自动化（grep）
- **输入**: `skill/SKILL.md` Step 5「软性素质缺口定向探查」
- **预期**: 触发判定式含 `(隐式 AND 权重 ∈ {高, 红线}) OR Step 3 缺口标记`；明示隐式高权重 / 红线项**不依赖 Step 3 先标缺口**即进必探查池。
- **关联**: 业务 AC-4 / 验证跑改进项 #1

### TC-T27: 简历 AI 润色三联征 → 诚信高风险面试必问（AC-3 加强）
- **类型**: 集成 / 自动化（grep）
- **输入**: `skill/SKILL.md` Step 3c「要点」
- **预期**: 含「诚信高风险三联征」（疑似 AI 润色 / 量化无 baseline / 角色措辞含糊）→ 标「诚信高风险」+ 面试「必问」指标含义 + baseline；并声明「只标高风险、不预判造假」。
- **关联**: 业务 AC-3 / 验证跑改进项 #2

### TC-T28: Step6 判定列禁用 🟡（只三态）
- **类型**: 边界 / 自动化（grep）
- **输入**: `skill/references/候选人横向对比-示例.md`「软性素质横向对比」段
- **预期**: 该段判定单元格只出现 ✅ / ❌ / ❓，不出现 🟡（🟡 仅合规于 Step 3 简历证据「信号强度」列与知识层覆盖表）。
- **关联**: 代码审查 minor-1 / 改进项 #3

### TC-T29: ❌缺失 vs ❓未覆盖 语义切分
- **类型**: 业务 / 关键（数据流）
- **输入**: `skill/SKILL.md` 三态判定规则 + `软性素质要求×表现对比-示例.md` 第 ③ 段抗压行
- **预期**: 规则明示 ❌缺失 = 已探查仍无证据（进风险点）；❓未覆盖 = 压根没探到（中性，反哺 Step 5）；示例「面试全程未涉及」的抗压标 ❓未覆盖而非 ❌缺失。
- **关联**: 业务 TC-016 / 改进项 #4

### TC-T30: 对外输出脱敏（库外 cwd 产物）契约存在
- **类型**: 流程 / 合规
- **输入**: `docs/privacy.md` + `scripts/check-pii.sh`
- **预期**: `docs/privacy.md` 声明 check-pii.sh 只覆盖仓库根、库外 cwd 产物需脱敏自检 / 落点契约；`check-pii.sh` 支持可选位置参数对任意目录扫描（无参数时行为与旧版一致）。
- **关联**: 业务 TC-026 / 改进项 #9 / #19

---

## 覆盖度自检（更新）
- 集成 / 可移植：TC-T08 / TC-T09 / TC-T13 / TC-T14 / TC-T15 / TC-T20 / TC-T24 / TC-T25 / TC-T26 / TC-T27
- 边界（白名单 / 自排他 / 不误伤 / 文件数受控 / 符号一致）：TC-T02 / TC-T04 / TC-T05 / TC-T06 / TC-T12 / TC-T16 / TC-T28
- 错误 / 防呆：TC-T03 / TC-T19 / TC-T21 / TC-T23 / TC-T29
- 流程门禁：TC-T01 / TC-T11 / TC-T30
- 合规声明：TC-T10
- 可移植契约：TC-T07 / TC-T22
- 软性素质签名 / 数据流 / 三态：TC-T17 / TC-T18 / TC-T19 / TC-T29
